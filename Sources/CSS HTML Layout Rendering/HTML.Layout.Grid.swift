//
//  HTML.Layout.Grid.swift
//  swift-css-html-layout-render
//

public import HTML_Rendering

extension HTML.Layout {
    /// Namespace for grid layout containers.
    ///
    /// Mirrors the L1 `Layout.Grid` shape; `Grid.Lazy` is the responsive
    /// column-template grid (formerly the top-level `LazyVGrid`).
    public enum Grid {}
}
